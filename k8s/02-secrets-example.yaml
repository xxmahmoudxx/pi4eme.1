# Committed TEMPLATE only. All values are placeholders.
# Real values live in k8s/local-secrets.yaml (gitignored).
# Copy this file to k8s/local-secrets.yaml and fill in real values, OR
# create the secret directly with: kubectl create secret generic pi4eme-secrets ...
apiVersion: v1
kind: Secret
metadata:
  name: pi4eme-secrets
  namespace: pi4eme
type: Opaque
stringData:
  # Backend
  JWT_SECRET: "REPLACE_ME_jwt_secret"
  TWO_FACTOR_ENCRYPTION_KEY: "REPLACE_ME_32_chars_padding______"
  GITHUB_CLIENT_ID: "REPLACE_ME_github_client_id"
  GITHUB_CLIENT_SECRET: "REPLACE_ME_github_client_secret"
  MAIL_USER: "REPLACE_ME_smtp_user@example.com"
  MAIL_PASS: "REPLACE_ME_smtp_app_password"
  GEMINI_API_KEY: "REPLACE_ME_gemini_api_key"

  # ML service (optional, can be empty if OCR not used in demo)
  OCR_API_KEY: ""
